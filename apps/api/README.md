# IndusGuard API

Backend FastAPI responsável pelo catálogo genérico de conectores OpenAPI e, nas próximas etapas,
pelo runtime do agente, políticas determinísticas, execução HTTP e observabilidade.

Consulte o README da raiz do monorepo para instalar, testar e executar o projeto.
